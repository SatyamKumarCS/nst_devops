upstream web_upstream {
    server web:80;
}

upstream api_upstream {
    server api:8080;
}

server {
    listen 80;
    server_name _;

    # Use Docker's internal DNS — re-resolve upstreams every 10s
    # Prevents stale IP caching when containers restart
    resolver 127.0.0.11 valid=10s ipv6=off;

    # Route /api/* to the backend API
    location /api/ {
        proxy_pass          http://api_upstream;
        proxy_set_header    Host              $host;
        proxy_set_header    X-Real-IP         $remote_addr;
        proxy_set_header    X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header    X-Forwarded-Proto $scheme;
    }

    # Route everything else to Next.js
    location / {
        proxy_pass          http://web_upstream;
        proxy_set_header    Host              $host;
        proxy_set_header    X-Real-IP         $remote_addr;
        proxy_set_header    X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header    X-Forwarded-Proto $scheme;

        # Required for Next.js WebSocket (HMR in dev)
        proxy_http_version  1.1;
        proxy_set_header    Upgrade    $http_upgrade;
        proxy_set_header    Connection "upgrade";
    }
}